\documentclass{standalone}
\usepackage{tikz}
\usepackage{ctex}
\setCJKmainfont{Noto Serif CJK SC}
\usepackage{mhchem,siunitx}
\begin{document}
\small
\begin{tikzpicture}
  \node {$\oplus$};
  \draw[semithick](-1,-1.4)rectangle(1,1.4);
  \foreach \x in {1,...,20}
  {
    \fill(360*rnd:{0.03*rnd*\x+0.15})circle(0.5pt);
  }
  \foreach \x in {1,...,20}
  {
    \fill(360*rnd:{0.03*rnd*\x+0.25})circle(0.5pt);
  }
  \foreach \x in {1,...,20}
  {
    \fill(360*rnd:{0.03*rnd*\x+0.3})circle(0.5pt);
  }
  \foreach \x in {1,...,20}
  {
    \fill(360*rnd:{0.03*rnd*\x+0.3})circle(0.5pt);
  }
  \foreach \x in {1,...,20}
  {
    \fill(360*rnd:{0.03*rnd*\x+0.35})circle(0.5pt);
  }
\end{tikzpicture}
\end{document}